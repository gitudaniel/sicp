(define (sum term a next b)
  (if (> a b)
      0
      (+ (term a)
	 (sum term (next a) next b))))
; The above is the abstraction of summation of numbers in a given range a to b
; Abstraction because it can be used to build various summation methods as we will see in sum_abstraction_application.scm
